# This project has no build step and no dependencies. Nothing here should ever
# need node_modules; if it appears, something went wrong.
node_modules/
package-lock.json

# Design aids from tools/preview-shapes.js — regenerate them, don't commit them.
# (tools/make-icons.js output IS committed: those ship with the app.)
preview-*.png

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
*.swp
